
features ... 
  catch file errors and display and handle
  ruler 
  context menus
  switch adding view panes to use workspace funcs
  auto-save when viewing replay of source with no archive
  ctrl-alt-a should go back one version when replay showing

behavior ...
  move down a line after toggling comment

bad behavior ...
  versions with no diff highlighting
  position drifts when navigating
  splitting panes hides replay dialog
  split panes -> multiple copies of editmgr
  update dialog when buffer has external changes
  stop diff srch on ver showing del marker, not empty next one
  show something on deletes in actual vers page

bugs ...
  git with diff
  editor crashes on split up with one source and one replay
  ctrl-z until tab empty and rewrite history warning shows -- press cancel -- atom closes
  v20 of package.json missing some hiliting

test ...
  test with all themes
  minimap
  